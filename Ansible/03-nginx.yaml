- name: installing nginx
  hosts: local
  become: yes
  connection: local
  tasks:
  - name: installing nginx tasks
    ansible.builtin.package:
      name: nginx
      state: present

  - name: start nginx
    ansible.builtin.services:
      name: nginx
      state: started
      enabled: yes